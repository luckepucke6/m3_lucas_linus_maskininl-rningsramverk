[project]
name = "m3-lucas-linus-maskininl-rningsramverk"
version = "0.1.0"
description = "ML inference API with FastAPI"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn[standard]"
]